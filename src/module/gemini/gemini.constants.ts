export const GEMINI_MODEL = 'gemini-flash-latest';

export const EXTRACT_TRANSACTIONS_PROMPT =
  'Extract every transaction from this bank statement text. Return only the transactions, nothing else.';
